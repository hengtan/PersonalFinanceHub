apiVersion: 1

providers:
  - name: "PFH Dashboards"
    orgId: 1
    folder: "Personal Finance Hub"
    folderUid: pfh-folder
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      # carrega todos os .json dessa pasta